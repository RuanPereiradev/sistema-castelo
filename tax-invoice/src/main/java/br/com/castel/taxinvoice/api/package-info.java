/**
 * The port through which the system asks for a tax invoice, and the types its signatures need.
 *
 * <p>Version 1 runs on a fake adapter. The shape here is deliberately the minimum that lets the port
 * exist: what a real issuer needs depends on how the property issues today, which the technical plan
 * lists as an open pendency for v1.2.
 */
package br.com.castel.taxinvoice.api;
